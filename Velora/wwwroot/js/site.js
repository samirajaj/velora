const menuButton=document.querySelector('[data-menu-button]');const menu=document.querySelector('[data-menu]');menuButton?.addEventListener('click',()=>{const open=menu.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));document.body.classList.toggle('menu-open',open)});
const searchButton=document.querySelector('[data-search-button]');const searchPanel=document.querySelector('[data-search-panel]');searchButton?.addEventListener('click',()=>{searchPanel.classList.toggle('open');if(searchPanel.classList.contains('open'))searchPanel.querySelector('input')?.focus()});
const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}}),{threshold:.08});document.querySelectorAll('.reveal').forEach(element=>observer.observe(element));
document.querySelectorAll('form[data-confirm]').forEach(form=>form.addEventListener('submit',event=>{if(!window.confirm(form.dataset.confirm))event.preventDefault()}));document.querySelectorAll('button[data-confirm]').forEach(button=>button.addEventListener('click',event=>{if(!window.confirm(button.dataset.confirm))event.preventDefault()}));
const variantList=document.querySelector('[data-variant-list]');const variantTemplate=document.querySelector('[data-variant-template]');document.querySelector('[data-add-variant]')?.addEventListener('click',()=>{const index=variantList.querySelectorAll('.variant-row').length;variantList.insertAdjacentHTML('beforeend',variantTemplate.innerHTML.replaceAll('__INDEX__',index))});document.addEventListener('click',event=>{if(event.target.matches('[data-remove-variant]'))event.target.closest('.variant-row')?.remove()});
const sortableImages=document.querySelector('[data-sortable-images]');let draggedImage=null;sortableImages?.addEventListener('dragstart',event=>{draggedImage=event.target.closest('.image-admin-card');draggedImage?.classList.add('dragging')});sortableImages?.addEventListener('dragend',()=>{draggedImage?.classList.remove('dragging');draggedImage=null});sortableImages?.addEventListener('dragover',event=>{event.preventDefault();const target=event.target.closest('.image-admin-card');if(target&&draggedImage&&target!==draggedImage){const box=target.getBoundingClientRect();sortableImages.insertBefore(draggedImage,event.clientX<box.left+box.width/2?target:target.nextSibling)}});
document.querySelector('[data-filter-toggle]')?.addEventListener('click',()=>document.querySelector('[data-filter-panel]')?.classList.toggle('open'));
